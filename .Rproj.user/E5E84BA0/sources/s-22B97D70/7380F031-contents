# read in data
ns = 10
data_dir = '_data/raw_data'

rawdata = c();
for (s in 1:ns) {
    # specify the path
    # read sub-level data using read.table sub_data = read.table()
    rawdata = rbind(rawdata, sub_data)
}

# plot
# complete x and y
qplot(x, y, geom = c('point','line'))